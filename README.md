# ScratchYouTube Copy

ScratchYouTube の実験用リポジトリ。

## V0.1

まずは GitHub Pages 上のページで `fetch()` フックを最初から読み込み、通信を検知できる構成を確認する。

- `index.html` — テストページ
- `hook.js` — `window.fetch` の監視

次の段階で Scratch VM を組み込む予定。
